# Brain Graph Index

Purpose: provide a deterministic map of reusable knowledge nodes.

## Authority / Redirect

This file is the Brain hub.
For global context authority, see `openclaw/CONTEXT_MAP.md`.
For canon authority, see `CEO.md`.

## What Brain Graph Is

Brain Graph is a node-based knowledge layer where each node has a clear purpose,
short canonical answers, and deep links to details.

## Navigation Layers

- Start here for system orientation.
- Move to protocol in `brain/01_BRAIN_PROTOCOL.md`.
- Use node template in `brain/02_NODE_TEMPLATE.md`.
- Use card schema in `brain/03_CARD_SCHEMA.md`.
- Follow writing conventions in `brain/04_WRITING_STYLE.md`.
- For OpenClaw/OTTO operations routing use `brain/domains/openclaw_ops/00_INDEX.md`.
- For universal corpus ingestion routing use `brain/domains/ingest/00_INDEX.md`.
- For MemoryOS (capture -> compact -> query) use `memory/00_INDEX.md`.

## Anti-Token Rule

Default behavior is index-first:

1. Read `brain/00_INDEX.md` and index outputs.
2. Read only top relevant nodes.
3. Do not read full vault/raw sources by default.

## New Knowledge Flow (Future)

`docs/_inbox/` -> cards -> node promotion.

## Validity by Competence

Technical knowledge does not expire by date alone.
It changes only when superseded, contradicted, or downgraded in status.

## Run Boundaries

- RUN 1: no migration of existing content.
- RUN 2: assimilation no-move via references and tagging.
- RUN 3+: vault/slices/compactor (future).
