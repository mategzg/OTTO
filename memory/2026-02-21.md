
- Se consolidó `MEMORY.md` como entrypoint de memoria estable y routing canónico a `memory/profile/*`, `memory/vision/*`, `brain/domains/personal_ops/*` y `brain/domains/sg_acabados/*`.
- Se reforzó Core Pack Hardening en archivos base (`AGENTS.md`, `SOUL.md`, `USER.md`, `HEARTBEAT.md`, `MEMORY.md`, `IDENTITY.md`, `TOOLS.md`) con estándar operable: Purpose, Decision scope, reglas, IO runtime y update triggers.
- Se ancló `https://docs.openclaw.ai/` como manual absoluto (fallback canónico) y se añadió protocolo de uso profundo en `brain/domains/openclaw_ops/17_OPENCLAW_DOCS_BIBLE_PROTOCOL.md`.
- Se creó mapeo operativo de docs OpenClaw por secciones (`cli/gateway/concepts/channels/tools/reference/providers`) para decidir rápidamente cuándo consultar cada parte.
- Se amplió indexación no paramétrica: memory index incluye `memory/profile/*.md` y `memory/vision/*.md`; se añadió stack de index/query para `repo_map` y se integró brain-query en `context_loader` para carga selectiva por señales de tema.
- Se fijó política de asimilación de export por fases en `state/export_assimilation_policy.json` + contrato en `docs/_inbox/export_assimilation_contract_latest.md`: ingesta -> limpieza -> clasificación por destino -> redacción curada -> index/verify.
- Regla de fase confirmada: conocimiento profesional (business/legal/marketing/sales/accountability/finance/customer service) va en `brain/domains/sg_acabados/*`; perfil en `memory/profile/*`; visión en `memory/vision/*`; aprendizaje operativo OTTO en `brain/domains/personal_ops/*`.
- Se reforzó doctrina de delegación: subagente supervisor por fase, cada supervisor delega a coder, espera handoff y recién reporta al owner; sin handoff no hay cierre válido.
- Se ajustó semántica de delegación heartbeat: faltante de handoff en trabajo efectivo se trata como intento fallido con bloqueo y causa raíz (no retry ciego); caso health-only puede cerrar sin handoff.
- Se ancló como canon de visión: Austin / Mission-Critical Production System Lead + plan SG->US->acquisition, Obra/Logos/accountability y marco capacidad-responsabilidad-estructura.
- Prioridad de fase explícita: homeostasis y soberanía como criterio rector #1.
- Sesgo operativo núcleo OTTO reforzado: automatización, delegación, leverage, compounding y optimización.

- Se reforzó estándar de auditoría: "verificado" solo con comando + output visible; si salida truncada/no observable -> GAP/NO_VERIFICADO + comando de confirmación.
- Se añadió stack de indexado para `repo_map` (`scripts/repomap_index_build.py`, `scripts/repomap_query.py`) y se dejó con tests de no-regresión.
- Se consolidó regla de asimilación de export por fases en política+contrato (ingesta -> limpieza -> clasificación destino -> redacción curada -> index/verify).
- Se reforzó contrato de delegación por fase: subagente supervisor por fase, espera handoff de coder, luego reporta; sin handoff no hay cierre válido.
- Se endureció semántica heartbeat/delegación: en trabajo efectivo sin handoff -> bloqueo/causa raíz primero (no retry ciego); health-only no fuerza handoff.
- Se consolidó Core Pack hardening con coherencia entre AGENTS/SOUL/USER/HEARTBEAT/MEMORY/IDENTITY/TOOLS y check PASS reportado en `docs/_inbox/core_pack_coherence_latest.*`.
- Se ancló protocolo “Docs Bible” de OpenClaw (`brain/domains/openclaw_ops/17_OPENCLAW_DOCS_BIBLE_PROTOCOL.md`) y mapa por secciones para consulta precisa.
- Se reforzó canon de visión como núcleo absoluto y se expandieron piezas canónicas (`70/80/85/90`) + prioridad explícita de fase: homeostasis y soberanía.
- Se ancló canon de meta técnica OTTO tipo “Supreme Architecture” en `brain/domains/openclaw_ops/18_OTTO_SUPREME_ARCHITECTURE_TARGET.md`, adaptado al proyecto (NL-first + evidence-first + RAG robusto + skills/recipes + estabilidad + evaluación).
