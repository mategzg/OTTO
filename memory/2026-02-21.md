
- Se consolidó `MEMORY.md` como entrypoint de memoria estable y routing canónico a `memory/profile/*`, `memory/vision/*`, `brain/domains/personal_ops/*` y `brain/domains/sg_acabados/*`.
- Se reforzó Core Pack Hardening en archivos base (`AGENTS.md`, `SOUL.md`, `USER.md`, `HEARTBEAT.md`, `MEMORY.md`, `IDENTITY.md`, `TOOLS.md`) con estándar operable: Purpose, Decision scope, reglas, IO runtime y update triggers.
- Se ancló `https://docs.openclaw.ai/` como manual absoluto (fallback canónico) y se añadió protocolo de uso profundo en `brain/domains/openclaw_ops/17_OPENCLAW_DOCS_BIBLE_PROTOCOL.md`.
- Se creó mapeo operativo de docs OpenClaw por secciones (`cli/gateway/concepts/channels/tools/reference/providers`) para decidir rápidamente cuándo consultar cada parte.
- Se amplió indexación no paramétrica: memory index incluye `memory/profile/*.md` y `memory/vision/*.md`; se añadió stack de index/query para `repo_map` y se integró brain-query en `context_loader` para carga selectiva por señales de tema.
- Se fijó política de asimilación de export por fases en `state/export_assimilation_policy.json` + contrato en `docs/_inbox/export_assimilation_contract_latest.md`: ingesta -> limpieza -> clasificación por destino -> redacción curada -> index/verify.
- Regla de fase confirmada: conocimiento profesional (business/legal/marketing/sales/accountability/finance/customer service) va en `brain/domains/sg_acabados/*`; perfil en `memory/profile/*`; visión en `memory/vision/*`; aprendizaje operativo OTTO en `brain/domains/personal_ops/*`.
- Se reforzó doctrina de delegación: subagente supervisor por fase, cada supervisor delega a coder, espera handoff y recién reporta al owner; sin handoff no hay cierre válido.
- Se ajustó semántica de delegación heartbeat: faltante de handoff en trabajo efectivo se trata como intento fallido con bloqueo y causa raíz (no retry ciego); caso health-only puede cerrar sin handoff.
- Se ancló como canon de visión: Austin / Mission-Critical Production System Lead + plan SG->US->acquisition, Obra/Logos/accountability y marco capacidad-responsabilidad-estructura.
- Prioridad de fase explícita: homeostasis y soberanía como criterio rector #1.
- Sesgo operativo núcleo OTTO reforzado: automatización, delegación, leverage, compounding y optimización.

- Se reforzó estándar de auditoría: "verificado" solo con comando + output visible; si salida truncada/no observable -> GAP/NO_VERIFICADO + comando de confirmación.
- Se añadió stack de indexado para `repo_map` (`scripts/repomap_index_build.py`, `scripts/repomap_query.py`) y se dejó con tests de no-regresión.
- Se consolidó regla de asimilación de export por fases en política+contrato (ingesta -> limpieza -> clasificación destino -> redacción curada -> index/verify).
- Se reforzó contrato de delegación por fase: subagente supervisor por fase, espera handoff de coder, luego reporta; sin handoff no hay cierre válido.
- Se endureció semántica heartbeat/delegación: en trabajo efectivo sin handoff -> bloqueo/causa raíz primero (no retry ciego); health-only no fuerza handoff.
- Se consolidó Core Pack hardening con coherencia entre AGENTS/SOUL/USER/HEARTBEAT/MEMORY/IDENTITY/TOOLS y check PASS reportado en `docs/_inbox/core_pack_coherence_latest.*`.
- Se ancló protocolo “Docs Bible” de OpenClaw (`brain/domains/openclaw_ops/17_OPENCLAW_DOCS_BIBLE_PROTOCOL.md`) y mapa por secciones para consulta precisa.
- Se reforzó canon de visión como núcleo absoluto y se expandieron piezas canónicas (`70/80/85/90`) + prioridad explícita de fase: homeostasis y soberanía.
- Se ancló canon de meta técnica OTTO tipo “Supreme Architecture” en `brain/domains/openclaw_ops/18_OTTO_SUPREME_ARCHITECTURE_TARGET.md`, adaptado al proyecto (NL-first + evidence-first + RAG robusto + skills/recipes + estabilidad + evaluación).
- Se implementó `scripts/nl_skill_router.py` (MVP) con decisión NL->tool/skill/workflow y heurística de creación de skill basada en repetición/impacto/riesgo.
- Se añadió `state/skill_creation_policy.json` como policy explícita para creación/defer de skills.
- Se agregaron pruebas de no-regresión en `tests/test_nl_skill_router.py` y quedaron verdes junto con `test_nl_intent_classifier.py`.
- Se actualizó `brain/domains/openclaw_ops/00_INDEX.md` para enrutar explícitamente al nuevo router NL y su policy.
- Se integró el NL Skill Router MVP en `scripts/channel_ingress_adapter.py`; los reportes de ingress ahora incluyen `nl_skill_route` con decisión interna de ruta (tool/skill/workflow/create/defer).
- Validación aplicada: `py_compile` + tests `test_channel_ingress_adapter.py` y `test_nl_skill_router.py` en verde.
- Mateo habilitó plan ChatGPT Pro para OTTO (alto presupuesto de uso), con intención explícita de operar sin microvigilancia de cuota cada pocos minutos y aumentar capacidad sostenida.
- Nueva preferencia owner (Telegram): OTTO debe auto-disparar compaction potente/proactivo cuando detecte presión alta de contexto para evitar estallido de ventana y caídas de respuesta; además mantener una sola respuesta por mensaje tras resets.
- Nueva directriz owner: en Telegram/Discord priorizar potencia/calidad sobre ahorro estricto de tokens; remover límites rígidos de contexto en esos canales (modo high-power).
- Directriz de comunicación owner: responder siempre en lenguaje natural simple, conciso y preciso (sin asumir perfil coder experto); esta preferencia debe persistirse en artefactos del sistema, no depender de contexto conversacional.
- SPEC-002 M2 iniciado/cerrado: se implementó registry formal de skills/recipes con lifecycle explícito (draft/test/enabled/deprecated/retired), validación de transiciones y enforcement en NL router con fallback seguro cuando un target no está registrado o no está enabled.
- SPEC-002 M3 cerrado: heurística ejecutable e instrumentada para creación de recipe/skill (repetición, impacto, generalidad, riesgo, costo/latencia) con scoring configurable por policy y auditoría NDJSON por decisión (`logs/skill_creation_decisions.ndjson`).
- SPEC-002 M4 cerrado: se implementó circuit breaker global + cooldown (`scripts/circuit_breaker.py`, `state/circuit_breaker_policy.json`) con umbrales por error-rate, latencia p95 y timeouts; integrado al NL router para degradación segura en cooldown (fallback `NO_VERIFICADO`/ruta segura) y recuperación half-open -> closed.
- SPEC-002 M5 cerrado: observabilidad end-to-end mínima con `trace_id` y métricas agregadas (latencia p50/p95, router success, tool success, retrieval hit/groundedness proxy, retry rate, token estimate) vía `scripts/observability.py`, eventos NDJSON (`logs/observability_events.ndjson`) y resumen latest (`docs/_inbox/observability_summary_latest.json`).
- SPEC-002 M6 cerrado: se implementó golden set + runner de regresión automática (`state/golden_set_spec002.json`, `scripts/golden_regression_runner.py`) con reportes deterministas (`docs/_inbox/golden_regression_latest.json|.md`, `logs/golden_regression_runs.ndjson`) y métricas proxy nDCG/MRR/groundedness; baseline actual quedó en estado `degraded` (pass_rate 0.6) para mejora en M7.
- SPEC-002 M7 cerrado: cobertura semántica profunda para SG con policy de sinónimos/queries (`state/semantic_coverage_policy.json`) + runner de evaluación por chunks estructurales (`scripts/semantic_coverage_runner.py`) y reportes (`docs/_inbox/semantic_coverage_latest.json|.md`); resultado actual `ok` con recall_proxy 1.0 en queries representativas configuradas.
- Cierre elite SPEC-002 validado: golden regression pasó 5/5 (pass_rate 1.0; nDCG/MRR/groundedness 1.0), observabilidad con router/tool success 1.0, semantic coverage recall_proxy 1.0. Evidencia consolidada en `docs/_inbox/spec002_elite_validation_latest.{json,md}`.
- Directriz de estilo owner incorporada en core: SOUL/USER actualizados para tono con postura clara, brevedad obligatoria, sin aperturas de relleno y franqueza con humor natural cuando corresponda.
- Nueva directriz de delegación owner: cancelar uso por defecto de coder CLIs externos; priorizar subagentes OpenClaw (`sessions_spawn`) por misión. Paralelización permitida solo con scopes segmentados para evitar conflictos.
- SPEC-003 M0 implementado: `scripts/release_audit.py` + `Makefile audit` + `tests/test_release_audit.py`; gate ahora falla de forma determinista si repo está dirty, faltan artefactos requeridos o fallan tests críticos.
- SPEC-003 M1 implementado: Retrieval Service v2 con contrato único (`scripts/retrieval_service.py`) e integración al NL router bajo feature flag (`state/retrieval_policy.json` -> `retrieval_v2.enabled`). Se añadió coverage de contrato e integración en tests (`tests/test_retrieval_service.py`, `tests/test_nl_skill_router.py`).
- SPEC-003 M2 implementado: pipeline hybrid retrieval E2E (lexical + vector + union/dedupe + fusion RRF/weighted + rerank + packer constraints) con diagnósticos por etapa (`lexical_candidates`, `vector_candidates`, `union_count`, `rerank_model`) y pruebas dedicadas (`tests/test_retrieval_hybrid_pipeline.py`).
- SPEC-003 M3 implementado: ACL pre-topK en RetrievalService (allow/deny por chunk con metadata `acl_allow`/`acl_deny`, enforcement antes de candidates/topK) + pruebas anti-fuga (`tests/test_acl_no_leak_candidates.py`, `tests/test_acl_no_leak_citations.py`).
- SPEC-003 M4 implementado: Evidence Guard universal (`scripts/evidence_guard.py`) integrado como última validación en NL router (`evidence_guard` en output). Si modo grounded no tiene evidencia suficiente => `NO_VERIFICADO` + `gaps`. Pruebas: `tests/test_evidence_guard.py`.
- SPEC-003 M5 implementado: breaker integrado en heartbeat/jobs con gating por recurso (`cron_jobs`, `tooling`, `retrieval`) antes de delegación/etapas pesadas; si `cron_jobs` está open se marca `cooldown_skip` y jobs dependientes quedan `skipped_cooldown`. Evidencia en `tests/test_breaker_blocks_jobs.py`.
